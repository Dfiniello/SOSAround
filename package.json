{
  "name": "sosaround-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "SOSAround — monorepo client (Expo/React Native) + server (Express/Socket.IO)",
  "scripts": {
    "client": "npm --prefix client start",
    "server": "npm --prefix server run dev",
    "install:all": "npm --prefix client install && npm --prefix server install"
  }
}
